<!doctype html>
<html lang="tr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Tüm Yollar Roma'ya Çıkar — MVP</title>
  <style>
    :root {
      --bg: #0d1115;
      --panel: rgba(17, 22, 27, .93);
      --panel2: rgba(30, 37, 44, .96);
      --text: #f3efe6;
      --muted: #a8b2bd;
      --line: rgba(255,255,255,.13);
      --gold: #d8b56a;
      --good: #72d09a;
      --bad: #e87678;
      --warn: #e1b85d;
    }
    * { box-sizing: border-box; }
    html, body { margin: 0; width: 100%; height: 100%; overflow: hidden; background: var(--bg); color: var(--text); font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
    button { font: inherit; }
    #app { position: relative; width: 100vw; height: 100vh; }
    #board { position: absolute; inset: 0; }
    #board canvas { display: block; width: 100%; height: 100%; }

    .panel {
      position: absolute;
      z-index: 5;
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 15px;
      backdrop-filter: blur(13px);
      box-shadow: 0 14px 34px rgba(0,0,0,.25);
    }
    button {
      appearance: none;
      color: var(--text);
      border: 1px solid var(--line);
      background: var(--panel2);
      border-radius: 10px;
      padding: 9px 11px;
      cursor: pointer;
      transition: border-color .15s, transform .08s, opacity .15s;
    }
    button:hover:not(:disabled) { border-color: rgba(255,255,255,.34); }
    button:active:not(:disabled) { transform: translateY(1px); }
    button:disabled { opacity: .4; cursor: not-allowed; }
    button.primary { background: #745a2d; border-color: #a27b39; }
    button.active { border-color: var(--gold); box-shadow: inset 0 0 0 1px rgba(216,181,106,.25); }
    button.danger { color: #ffb3b5; }

    #topbar {
      top: 14px; left: 14px; right: 14px;
      min-height: 64px;
      padding: 10px 12px;
      display: flex; align-items: center; justify-content: space-between; gap: 14px;
    }
    .titlebox h1 { margin: 0; font-size: 15px; font-weight: 750; }
    .titlebox p { margin: 3px 0 0; color: var(--muted); font-size: 11px; }
    .topmetrics { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; justify-content: flex-end; }
    .chip { border: 1px solid var(--line); border-radius: 9px; padding: 7px 9px; font-size: 11px; color: var(--muted); background: rgba(255,255,255,.025); }
    .chip b { color: var(--text); font-weight: 700; margin-left: 4px; }
    .chip.urgent { border-color: rgba(225,184,93,.55); color: #f2d793; }

    #leftPanel { top: 92px; left: 14px; width: 220px; padding: 10px; }
    #rightPanel { top: 92px; right: 14px; width: 225px; padding: 10px; }
    .panel h2 { margin: 0 0 9px; font-size: 12px; font-weight: 750; letter-spacing: .02em; }
    .currentPlayer { border: 1px solid var(--line); border-radius: 12px; padding: 10px; margin-bottom: 10px; }
    .currentPlayer .name { display: flex; justify-content: space-between; gap: 8px; align-items: center; }
    .dot { width: 10px; height: 10px; border-radius: 50%; display: inline-block; margin-right: 6px; vertical-align: -1px; }
    .currentPlayer .name strong { font-size: 13px; }
    .ap { margin-top: 8px; display: flex; gap: 6px; }
    .ap span { width: 22px; height: 8px; border-radius: 999px; background: rgba(255,255,255,.1); }
    .ap span.on { background: var(--good); }

    .players { display: grid; gap: 7px; }
    .playerRow { border: 1px solid var(--line); border-radius: 10px; padding: 8px; font-size: 11px; background: rgba(255,255,255,.02); }
    .playerRow.on { border-color: rgba(255,255,255,.28); }
    .playerHead { display: flex; align-items: center; justify-content: space-between; gap: 8px; margin-bottom: 5px; }
    .playerStats { display: flex; gap: 8px; color: var(--muted); flex-wrap: wrap; }
    .route { margin-top: 5px; font-size: 10px; color: var(--muted); }
    .route.connected { color: #bfeccf; }

    .actionGrid { display: none; }
    #modeText { min-height: 45px; border: 1px solid var(--line); border-radius: 10px; padding: 8px; color: var(--muted); font-size: 11px; line-height: 1.4; }
    #selectedInfo { margin-top: 8px; font-size: 11px; color: var(--muted); line-height: 1.4; }

    #log { margin-top: 11px; padding-top: 10px; border-top: 1px solid rgba(255,255,255,.08); }
    #logList { display: grid; gap: 5px; max-height: 215px; overflow: hidden; }
    .logItem { font-size: 10px; color: var(--muted); line-height: 1.35; }
    .logItem strong { color: var(--text); }

    #hand {
      left: 245px; right: 245px; bottom: 14px;
      padding: 9px 10px 10px;
      max-height: 265px;
    }
    #handWorkbench { display:grid; grid-template-columns: 110px 1fr 132px 132px; gap:8px; align-items:stretch; }
    .dropZone { border:1px dashed var(--line); border-radius:11px; min-height:92px; padding:8px; background:rgba(255,255,255,.025); display:flex; flex-direction:column; justify-content:center; gap:4px; transition:.15s; }
    .dropZone strong { font-size:11px; }
    .dropZone small { color:var(--muted); font-size:9px; line-height:1.3; }
    .dropZone.active { border-color:var(--gold); background:rgba(216,181,106,.08); }
    .dropZone.good { border-color:var(--good); background:rgba(114,208,154,.08); }
    .dropZone.bad { border-color:var(--bad); background:rgba(232,118,120,.08); }
    #deckZone { cursor:pointer; text-align:center; }
    #prevDiscardCards { display:flex; gap:4px; flex-wrap:wrap; max-height:58px; overflow:hidden; }
    .miniDiscard { border:1px solid var(--line); border-radius:7px; padding:4px 6px; font-size:9px; background:rgba(255,255,255,.04); cursor:grab; }
    .miniDiscard:active { cursor:grabbing; }
    #turnDiscardZone { text-align:center; }
    #turnDiscardZone .bigIcon { font-size:22px; }
    .handHead { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 8px; }
    .handHead h2 { margin: 0; }
    .handHead span { color: var(--muted); font-size: 10px; }
    #handCards {
      display: grid;
      grid-template-columns: repeat(7, minmax(0, 1fr));
      gap: 8px;
      padding-bottom: 2px;
    }
    .handCard {
      min-width: 0; min-height: 64px; text-align: center; position: relative; overflow: hidden;
      background: linear-gradient(180deg, rgba(255,255,255,.055), rgba(255,255,255,.025));
      cursor: grab; user-select:none;
      display:flex; flex-direction:column; align-items:center; justify-content:center;
    }
    .handCard:active { cursor:grabbing; }
    .handCard.dragging { opacity:.42; border-color:var(--gold); }
    .handCard .icon { font-size: 22px; display: block; margin-bottom: 5px; }
    .handCard strong { font-size: 12px; display: block; }
    .handCard.selected { border-color: var(--gold); background: rgba(216,181,106,.09); }
    .handCard.tactic { border-style: dashed; }

    #cameraHint {
      position: absolute; z-index: 4; left: 50%; bottom: 145px; transform: translateX(-50%);
      color: rgba(243,239,230,.62); font-size: 10px; pointer-events: none;
      text-shadow: 0 1px 3px rgba(0,0,0,.7);
    }

    #toast { position: absolute; z-index: 12; left: 50%; top: 88px; transform: translateX(-50%); padding: 8px 12px; border-radius: 999px; border: 1px solid var(--line); background: rgba(8,11,14,.92); font-size: 11px; opacity: 0; pointer-events: none; transition: opacity .15s; }
    #toast.show { opacity: 1; }

    #modalShade { position: absolute; inset: 0; z-index: 20; background: rgba(0,0,0,.55); display: none; align-items: center; justify-content: center; padding: 18px; }
    #modalShade.show { display: flex; }
    #modal { width: min(620px, 100%); background: #171d23; border: 1px solid var(--line); border-radius: 16px; padding: 15px; box-shadow: 0 22px 60px rgba(0,0,0,.45); }
    #modal h3 { margin: 0 0 5px; font-size: 14px; }
    #modal p { margin: 0 0 11px; color: var(--muted); font-size: 11px; line-height: 1.4; }
    .modalRow { display: flex; justify-content: space-between; align-items: center; gap: 10px; margin: 10px 0; font-size: 11px; }
    .modalRow input[type="range"] { flex: 1; }
    .checkRow { display: flex; align-items: center; gap: 8px; border: 1px solid var(--line); border-radius: 10px; padding: 8px; margin: 7px 0; font-size: 11px; }
    .modalActions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 12px; }
    #gameOver { text-align: center; }
    #gameOver .crown { font-size: 34px; }


    #referencePanel { position:absolute; z-index:11; top:88px; right:14px; width:min(520px, calc(100vw - 28px)); max-height:calc(100vh - 180px); overflow:auto; display:none; padding:12px; background:var(--panel); border:1px solid var(--line); border-radius:14px; backdrop-filter:blur(13px); }
    #referencePanel.show { display:block; }
    #referencePanel table { width:100%; border-collapse:collapse; font-size:10px; }
    #referencePanel th, #referencePanel td { padding:7px 6px; border-bottom:1px solid rgba(255,255,255,.08); text-align:left; vertical-align:top; }
    #referencePanel th { color:var(--muted); font-weight:600; }
    #referencePanel .refHead { display:flex; justify-content:space-between; align-items:center; gap:8px; margin-bottom:8px; }

    @media (max-width: 1100px) {
      #leftPanel { width: 205px; }
      #rightPanel { width: 215px; }
      #hand { left: 215px; right: 225px; }
      #handWorkbench { grid-template-columns: 92px 1fr 110px 110px; }
      #handCards { grid-template-columns: repeat(4, minmax(0, 1fr)); }
      #cameraHint { bottom: 215px; }
    }
    @media (max-width: 760px) {
      #leftPanel, #rightPanel { display: none; }
      #hand { left: 10px; right: 10px; }
      #handWorkbench { grid-template-columns: 80px 1fr 92px; }
      #prevDiscardZone { display:none; }
      #handCards { grid-template-columns: repeat(4, minmax(0, 1fr)); }
    }
  </style>
</head>
<body>
<div id="app">
  <div id="board"></div>

  <header id="topbar" class="panel">
    <div class="titlebox">
      <h1>Tüm Yollar Roma'ya Çıkar — oynanabilir MVP</h1>
      <p>Kartı sürükle → bırak · Roma 4 Yol uzakta · Kale level = +1 savunma/+3 kapasite · Asker yalnız tamamlanmış yolda yürür</p>
    </div>
    <div class="topmetrics">
      <div class="chip">Tur <b id="roundValue">1</b></div>
      <div class="chip">Deste <b id="deckValue">0/100</b></div>
      <div class="chip" id="finalChip"><span id="finalLabel">Iskarta</span> <b id="finalValue">0</b></div>
      <div class="chip">Roma <b id="romeOwnerValue">Tarafsız</b></div>
      <button id="cardsRefBtn">Kartlar</button>
      <button id="endTurnBtn" style="display:none">Turu Bitir</button>
      <button id="resetBtn">↻</button>
    </div>
  </header>

  <aside id="leftPanel" class="panel">
    <div class="currentPlayer" id="currentPlayerBox">
      <div class="name"><strong id="currentPlayerName">Oyuncu 1</strong><span id="actionsText">7 karta tamamla</span></div>
      <div class="ap" id="apDots" style="display:none"><span></span><span></span></div>
    </div>
    <h2>Oyuncular</h2>
    <div id="players" class="players"></div>
  </aside>

  <aside id="rightPanel" class="panel">
    <h2>Nasıl oynanır?</h2>
    <div class="actionGrid">
      <button id="moveBtn">⚔️ Ordu Hareketi</button>
      <button id="cancelBtn">✕ Modu İptal</button>
    </div>
    <div id="modeText">Kartı elinden tahtaya sürükle. Kale kartını kendi kalenin üstüne bırakınca level artar. Lejyon yalnız iki ucu Kale/Ana Kale/Roma ile kapanmış parlak yol segmentlerinde hareket eder.</div>
    <div id="selectedInfo">Seçili bölge: —</div>

    <div id="log">
      <h2>Oyun Günlüğü</h2>
      <div id="logList"></div>
    </div>
  </aside>

  <section id="hand" class="panel">
    <div class="handHead">
      <h2>Elindeki kartlar</h2>
      <span id="handHint">Tur başında elini 7 karta tamamla · En fazla 3 kart oyna · 1 kart atarak turu bitir</span>
    </div>
    <div id="handWorkbench">
      <div id="deckZone" class="dropZone"><span style="font-size:22px">🂠</span><strong>Kapalı Deste</strong><small id="deckZoneText">Kart almak için tıkla</small></div>
      <div id="handCards"></div>
      <div id="prevDiscardZone" class="dropZone"><strong>Önceki Iskarta</strong><small>Refill sırasında buradan kart al</small><div id="prevDiscardCards"></div></div>
      <div id="turnDiscardZone" class="dropZone"><span class="bigIcon">🗑️</span><strong>Turu Bitir</strong><small>Elinden 1 kartı buraya sürükle</small></div>
    </div>
  </section>

  <section id="referencePanel">
    <div class="refHead"><strong>100 Kartlık Deste</strong><button id="closeRefBtn">Kapat</button></div>
    <table><thead><tr><th>Kart</th><th>Adet</th><th>Ne işe yarar?</th></tr></thead><tbody id="referenceTableBody"></tbody></table>
  </section>

  <div id="cameraHint">Boş alanda sol sürükle: kamera · Sağ sürükle: kaydır · Tekerlek: zoom · Kartı tahtaya sürükle · Lejyonu komşu karta sürükle</div>
  <div id="toast" aria-live="polite"></div>

  <div id="modalShade">
    <div id="modal"></div>
  </div>
</div>

<script type="module">
  import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
  import { OrbitControls } from 'https://cdn.jsdelivr.net/npm/three@0.180.0/examples/jsm/controls/OrbitControls.js/+esm';

  const CELL = 1.65;
  const TILE = 1.46;
  const HALF = TILE / 2;
  const ROAD_WIDTH = .38;
  const DIRS = [
    { dx: 0, dz: -1, side: 'N', opp: 2 },
    { dx: 1, dz: 0, side: 'E', opp: 3 },
    { dx: 0, dz: 1, side: 'S', opp: 0 },
    { dx: -1, dz: 0, side: 'W', opp: 1 },
  ];
  const PLAYER_COLORS = [0x000000, 0xc66f68, 0x5f91d5, 0x6fae77, 0xb281c7];
  const PLAYER_CSS = ['', '#c66f68', '#5f91d5', '#6fae77', '#b281c7'];
  const PLAYER_NAMES = ['', 'Oyuncu 1', 'Bot 1', 'Bot 2', 'Bot 3'];
  const HUMAN_PLAYER_ID = 1;
  const MAX_CARDS_PER_TURN = 3;
  const TOTAL_CARDS = 100;
  const REACTION_CARD_TYPES = new Set(['tactic','siege','shield']);
  const CARD_COUNTS = { road:25, fort:20, legion:25, reinforcement:8, tactic:8, siege:7, shield:7 };
  const CARD_INFO = {
    road:          { icon: '🛣️', label: 'Yol', desc: 'Düz yol. Asker ancak iki ucu Kale/Ana Kale/Roma ile tamamlanmış yol segmentlerinde hareket edebilir.' },
    fort:          { icon: '🏰', label: 'Kale', desc: 'Yeni kale kur veya kendi kalenin üstüne bırakıp seviye artır. Her seviye +1 savunma ve +3 asker kapasitesi verir.' },
    legion:        { icon: '⚔️', label: 'Lejyon', desc: 'Ana kalende +1 lejyon doğar; bu tur hareket edemez.' },
    reinforcement: { icon: '🪖', label: 'Takviye', desc: 'Ana kalende +2 lejyon doğar; 2 kart oynama hakkı harcar ve birlikler bu tur hareket edemez.' },
    tactic:        { icon: '🦅', label: 'Taktik', desc: 'Savaş desteği: saldırı veya savunmada +2.' },
    siege:         { icon: '🧨', label: 'Kuşatma', desc: 'Yalnız Kale veya Roma saldırısında +3 saldırı.' },
    shield:        { icon: '🛡️', label: 'Kalkan', desc: 'Savunma reaksiyonu: savaşta +3 savunma.' },
  };

  const boardEl = document.getElementById('board');
  const handEl = document.getElementById('handCards');
  const handHintEl = document.getElementById('handHint');
  const playersEl = document.getElementById('players');
  const logListEl = document.getElementById('logList');
  const roundValueEl = document.getElementById('roundValue');
  const deckValueEl = document.getElementById('deckValue');
  const finalChipEl = document.getElementById('finalChip');
  const finalLabelEl = document.getElementById('finalLabel');
  const finalValueEl = document.getElementById('finalValue');
  const romeOwnerValueEl = document.getElementById('romeOwnerValue');
  const currentPlayerNameEl = document.getElementById('currentPlayerName');
  const currentPlayerBoxEl = document.getElementById('currentPlayerBox');
  const actionsTextEl = document.getElementById('actionsText');
  const apDotsEl = document.getElementById('apDots');
  const modeTextEl = document.getElementById('modeText');
  const selectedInfoEl = document.getElementById('selectedInfo');
  const moveBtn = document.getElementById('moveBtn');
  const cancelBtn = document.getElementById('cancelBtn');
  const endTurnBtn = document.getElementById('endTurnBtn');
  const resetBtn = document.getElementById('resetBtn');
  const cardsRefBtn = document.getElementById('cardsRefBtn');
  const closeRefBtn = document.getElementById('closeRefBtn');
  const referencePanel = document.getElementById('referencePanel');
  const referenceTableBody = document.getElementById('referenceTableBody');
  const deckZone = document.getElementById('deckZone');
  const deckZoneText = document.getElementById('deckZoneText');
  const prevDiscardZone = document.getElementById('prevDiscardZone');
  const prevDiscardCards = document.getElementById('prevDiscardCards');
  const turnDiscardZone = document.getElementById('turnDiscardZone');
  const toastEl = document.getElementById('toast');
  const modalShade = document.getElementById('modalShade');
  const modalEl = document.getElementById('modal');

  const scene = new THREE.Scene();
  scene.background = new THREE.Color(0x0e1317);
  scene.fog = new THREE.Fog(0x0e1317, 26, 58);

  const camera = new THREE.PerspectiveCamera(48, 1, .1, 120);
  camera.position.set(11, 20, 19);

  const renderer = new THREE.WebGLRenderer({ antialias: true });
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  renderer.shadowMap.enabled = true;
  renderer.shadowMap.type = THREE.PCFSoftShadowMap;
  boardEl.appendChild(renderer.domElement);

  const controls = new OrbitControls(camera, renderer.domElement);
  controls.target.set(.5 * CELL, 0, .5 * CELL);
  controls.enableDamping = true;
  controls.dampingFactor = .075;
  controls.minDistance = 7;
  controls.maxDistance = 48;
  controls.minPolarAngle = .18;
  controls.maxPolarAngle = Math.PI * .49;
  controls.zoomToCursor = true;
  controls.screenSpacePanning = true;
  controls.mouseButtons.LEFT = THREE.MOUSE.ROTATE;
  controls.mouseButtons.MIDDLE = THREE.MOUSE.DOL